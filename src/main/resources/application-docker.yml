server:
  port: 8080

# Services
external-services:
  social:
    service:
      url: http://pump-social-service:8080

# Logging
logging:
  level:
    com.johnmartin.auth: INFO

spring:
  jpa:
    hibernate:
      ddl-auto: update
